/**
 * Page Builder - Re-export from new builder architecture
 * The old builder has been replaced with a new tree-based architecture
 * using Zustand + zundo + @dnd-kit
 */

export { Builder as default } from '../../../../builder/index.tsx';
